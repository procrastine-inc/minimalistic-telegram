import 'package:flutter/material.dart';

class UnsupportedSubtitle extends StatelessWidget {
  const UnsupportedSubtitle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Unsupported.");
  }
}
